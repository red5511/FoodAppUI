import { TruncateOnResizePipe } from './truncate-on-resize.pipe';

describe('TruncateOnResizePipe', () => {
  it('create an instance', () => {
    const pipe = new TruncateOnResizePipe();
    expect(pipe).toBeTruthy();
  });
});
